# Example — whitelisted institutional pool

Wires `WhitelistBorrow` + `ReputationRate` onto a Solend reserve and replays a small event stream where the borrower is on the allowlist and the borrower is not on the allowlist.

```
ts-node index.ts
```
